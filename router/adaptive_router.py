# router/adaptive_router.py — Outcome-adaptive routing (STRETCH GOAL)
# TODO: if routed to cheap model and eval score drops below threshold
# → automatically escalate routing boundary for that payload class
# Feedback control loop — not a static config file
