# config/prometheus.yml
global:
  scrape_interval: 10s

scrape_configs:
  - job_name: "llmgateway"
    static_configs:
      - targets: ["gateway:8000"] # Points to the Docker network name of your API
